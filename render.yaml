services:
  - type: web
    name: IPLproject
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port $PORT --server.enableCORS false
    autoDeploy: truegit add render.yaml
